<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/staff_res"
        android:label="@string/app_name"
        android:roundIcon="@drawable/staff_res"
        android:supportsRtl="true"
        android:theme="@style/Theme.StaffApp"
        tools:targetApi="31">

        <activity android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CreateAccountActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.PatientActivity" />
        <activity android:name=".activities.PatientDetailActivity" />
        <activity android:name=".activities.VitalsHistoryActivity" />
        <activity android:name=".activities.AllergiesHistoryActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.AppointmentActivity" />
        <activity android:name=".activities.AppointmentDetailsActivity" />
        <activity android:name=".activities.EarningActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.EditProfileActivity" />
    </application>

</manifest>